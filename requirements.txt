# ============================================
# YAMA AI - Complete Requirements
# Pair with runtime.txt (python-3.11.9) so all
# these pinned wheels install without compiling.
# ============================================

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6   # <-- was MISSING. FastAPI's UploadFile/Form (used
                           #     by your /upload endpoint) need this at
                           #     runtime — without it every upload request
                           #     fails with "Form data requires
                           #     python-multipart to be installed."

# Search & Web
ddgs==9.0.1
requests==2.31.0
beautifulsoup4==4.12.2
feedparser==6.0.10

# Database
tinydb==4.8.0

# Mathematics & Science
sympy==1.12
numpy==1.24.3
matplotlib==3.7.2
scipy==1.11.4
mpmath==1.3.0

# Chemistry
chempy==0.8.2
mendeleev==0.14.0

# Language Tools
deep-translator==1.11.4
language-tool-python==2.7.1   # needs a Java runtime on the host
nltk==3.8.1
langdetect==1.0.9

# Time & Date
pytz==2023.3
holidays==0.35

# Developer Utilities
rjsmin==1.2.0
rcssmin==1.1.0
python-whois==0.8.0
dnspython==2.4.2

# Productivity
qrcode==7.4.2
geopy==2.4.1
python-barcode==0.15.1

# OCR (Optional - for image text extraction)
Pillow==10.1.0
pytesseract==0.3.10   # also needs the Tesseract-OCR binary on the OS itself

# Additional Utilities
# pygments and markdown below are not imported anywhere in app.py right now
# (kept only if you're planning to use them soon - safe to delete otherwise)
pygments==2.16.1
markdown==3.5.1
