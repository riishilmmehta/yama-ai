# ============================================
# YAMA AI – Dependencies
# Trimmed to match what app.py actually imports.
# Packages that were listed but never imported
# (googlesearch-python, latex2sympy2, antlr4-python3-runtime,
#  PyMuPDF, python-docx, python-pptx, pandas, openpyxl,
#  yfinance, rapidfuzz, thefuzz, textblob, dateparser,
#  networkx, PyPDF2) have been removed — they just slow down
# install and increase the chance of a version conflict.
# ============================================

# ============ CORE ============
fastapi
uvicorn
requests
beautifulsoup4
tinydb
ddgs
python-multipart      # required by FastAPI for UploadFile / Form()

# ============ MATHEMATICS ENGINE ============
sympy
mpmath
numpy
scipy
matplotlib

# ============ CHEMISTRY ENGINE ============
chempy
mendeleev

# ============ LANGUAGE TOOLS ============
deep-translator
language-tool-python   # needs a Java runtime installed on the host
nltk
langdetect

# ============ TIME & DATE ============
pytz
holidays

# ============ DEVELOPER UTILITIES ============
rjsmin
rcssmin

# ============ CYBERSECURITY ============
python-whois
dnspython

# ============ PRODUCTIVITY ============
qrcode
geopy
python-barcode         # <-- was MISSING from your list; app.py does
                        #     `import barcode` which needs this package,
                        #     not "barcode" (that's a different, unrelated
                        #     PyPI package) — without it the app crashes
                        #     on startup with ModuleNotFoundError

# ============ DATA & APIS ============
feedparser              # used for RSS news fetching

# ============ IMAGE / OCR (optional, app degrades gracefully without it) ============
Pillow
pytesseract              # also requires the Tesseract-OCR binary on the
                          # OS itself (e.g. `apt install tesseract-ocr`
                          # on Debian/Ubuntu, or the Windows installer) —
                          # pip only installs the Python wrapper
